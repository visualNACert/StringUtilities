
target 'StringUtilities-iOS' do

	platform :ios, '8.0'
	use_frameworks!

	pod 'StringUtilities', :path => '../'
	pod 'Nimble', '~> 7.0.0'

end

target 'StringUtilities-macOS' do

	platform :osx, '10.10'
	use_frameworks!

	pod 'StringUtilities', :path => '../'
	pod 'Nimble', '~> 7.0.0'

end
